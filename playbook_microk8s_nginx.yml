---

- hosts: microk8s
  gather_facts: yes
  become: yes

  vars:
    istio_version: 1.7.5

  roles:
    # this is not necessary, already supplied with microk8s ingress
    #- role: nginx-ingress

    - role: nginx-exposed
    - role: golang-hello-world-web
    #- role: hello-app-with-nginx-ingress

    #- role: istio-operator
    #- role: istio-gateway
